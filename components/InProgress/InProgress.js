import styles from './InProgress.module.css';

const InProgress = () => (
    <div className={styles.wrapper}>
        <p>Work in progress - does not represent the final look of the app</p>
    </div>
);

export default InProgress;